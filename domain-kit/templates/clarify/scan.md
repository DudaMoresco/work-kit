> Roteiro **interno** — curadoria inline no discover/scan. Não invocar `/domain.clarify`.

Para cada finding em `scan-manifest.json`:

- [ ] **Incorporar** — em qual artefato? (desafio, UL, requisitos, enunciado)
- [ ] **Ignorar** — motivo
- [ ] **Pendente** — precisa validação humana / Domain Expert

Nunca copiar texto longo sem citação `source` + `url`/`path`.

## Quando usar

Sempre **inline** no discover (fase 0) ou scan (re-sync). Não há comando clarify separado.
