# Rascunhos — {produto}

Pasta **não canônica**. Artefatos aqui aguardam aprovação antes de ir para o hub.

| Campo | Significado |
| --- | --- |
| `manifest.json` | Lista de promotes pendentes |
| Subpastas | Espelham path final (ex.: `01-product/…`) |

**Promover:** confirme no chat (`pode gravar`, `ok`) — o agente copia para o path canônico e remove daqui.

**Descartar:** peça para cancelar o rascunho.

Não referenciar `.draft/` em handoffs para etapas posteriores (fora deste kit).
